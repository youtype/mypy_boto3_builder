name: Code style and test

on:
  push:
    paths-ignore:
      - pyproject.toml
      - CHANGELOG.md

jobs:
  unit-tests:
    name: Code style & unit tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python 3.9
        uses: actions/setup-python@v1
        with:
          python-version: "3.9"
      - name: Cache packages
        uses: actions/cache@v2
        with:
          path: ~/.cache/pip
          key: 3.9-package-${{ hashFiles('**/Pipfile.lock') }}
          restore-keys: |
            3.9-package-
      - name: Install dependencies
        run: |
          python -m pip install poetry
          python -m poetry config virtualenvs.create false
          python -m poetry install --no-dev -n
      - name: Lint
        run: |
          pylint mypy_boto3_builder
          mypy mypy_boto3_builder
      - name: Test
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
        run: |
          pytest

  integration-tests:
    name: Integration tests
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [3.9]
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v1
        with:
          python-version: ${{ matrix.python-version }}
      - name: Cache packages
        uses: actions/cache@v2
        with:
          path: ~/.cache/pip
          key: ${{ matrix.python-version }}-package-${{ hashFiles('**/Pipfile.lock') }}
          restore-keys: |
            ${{ matrix.python-version }}-package-
      - name: Install dependencies
        run: |
          python -m pip install poetry
          python -m poetry config virtualenvs.create false
          python -m poetry install --no-dev -n
      - name: Integration tests
        run: |
          scripts/integration.sh
